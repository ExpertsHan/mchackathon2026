# 專案簡報與現場 Demo 指南

> 目標：讓評審在結束時能回答三件事：民眾怎麼使用、系統怎麼判斷、哪些決定仍由規則與承辦人掌握。

建議長度：**15 分鐘（簡報約 9 分鐘、Demo 約 5 分鐘、結尾約 1 分鐘）**。若主辦單位另有時限，保留第 2、5、7、8、9、12 頁與 Demo 的關鍵畫面，其他頁合併。

## 先定好要講的「一條主線」

用兩段相互補充的故事串起展示：第一段是「AI 網站提示 → Copilot 政策問答、收據與資安課程」；第二段是「LINE 進入 OCR 申請頁 → 文件比對 → 承辦人複核 → LINE 追蹤」。每次切畫面，都先說**目前在哪一套案件流程、站在誰的角度**。

### 實際系統關係：投影片務必畫準

```text
路徑 A（可直接展示的串接）
Chrome 擴充功能偵測網站
  └─「立即申請」帶產品名稱 → Copilot 申請網站 :3000
                                  ├─ 政策聊天：RAG → 政策段落／引用
                                  ├─ 收據、資格規則、4 個資安測驗、案件追蹤
                                  └─ 完整文件審核 → 共用 OCR 專案的辨識／規則模組

路徑 B（另一個可運作的申請入口）
LINE 官方帳號 → webhook → OCR／LINE 服務 :3001
  ├─「申請」→ apply.html?uid=LINE 使用者 ID → 上傳文件
  ├─ Gemini 擷取欄位 → RULE-001～020 → admin.html 人工複核
  └─「查詢進度」／案件狀態變更 → LINE 回覆／推播

擴充功能的「加 LINE」按鈕 → LINE 官方帳號連結
```

**講者應說明：**路徑 A 的 Copilot 案件與路徑 B 的 LINE／OCR 案件，目前是兩套各自儲存的示範流程；Copilot 可以呼叫 OCR 專案的程式模組做文件分析，但兩邊的案件編號及狀態不會自動同步。擴充功能使用的是本機 `localhost:3000` 連結及內建網域表。這是目前作品的實際邊界，也可作為未來整合成單一案件平台的方向。

## 一、依照你的五段分工安排投影片

| 段落 | 建議頁數／時間 | 聽眾要記住的事 | 交棒句 |
| --- | --- | --- | --- |
| ① 開場、總覽與三者連動 | 第 1～3 頁；約 2 分鐘 | 同一個補助情境有瀏覽器提示、網站辦理、LINE 互動三個入口 | 「接下來看民眾進入網站後，怎麼得到可信的說明。」 |
| ② AI 聊天、RAG、資安考題 | 第 4～6 頁；約 2.5 分鐘 | RAG 用於政策問答；考題是強制流程關卡，答案由後端判定 | 「回答完政策還不夠，文件也要能核對。」 |
| ③ 資料檢驗與 LINE Bot | 第 7～8 頁；約 2 分鐘 | OCR 擷取資料，規則逐項比對；LINE 負責入口、查詢與通知 | 「最後一個入口是使用者平常正在用 AI 時的瀏覽器。」 |
| ④ Chrome 擴充功能 | 第 9～10 頁；約 1.5 分鐘 | 以網域分類和輸入內容檢查，提醒風險並帶入申請 | 「現在用一個使用者故事，把三個入口實際走一次。」 |
| ⑤ Demo 與結尾 | 第 11～12 頁；約 6～7 分鐘 | 看見畫面、規則依據與人工責任 | 「我們完成的是可追溯的示範流程。」 |

若由不同隊員講，每人只需記住自己那段的**一句主張、兩個證據畫面、一句交棒句**。不要每位隊員重複講完整架構。

## 二、逐頁製作：投影片放什麼、現場說什麼

**共同版型：**16:9、簡潔底色、固定兩種主色（建議深藍與綠色；風險才用紅色）、一頁一個重點。標題至少約 32 pt，內文至少約 18 pt；截圖只裁到要證明的區域，必要時以放大框突出按鈕或結果。標題用結論或明確主題，不用「系統架構介紹」這類空泛字眼。以下的「台詞」放在講者備註，不要全部貼到投影片。

### 第 1 頁｜封面：AI 補助申請與安全審核示範

- **畫面：**專案名稱、團隊、三個小標籤「瀏覽器提醒／線上申請／LINE 服務」。註明「黑客松示範系統；Copilot 人物、政策與付款資料為虛構」。
- **台詞（15 秒）：**「我們把補助申請前的政策理解、申請中的安全教育，與申請後的文件審核和進度通知放進同一個使用者故事。」

### 第 2 頁｜使用者旅程：發現、申請、審核、追蹤

- **畫面：**橫向四步流程，依序放 Chrome 提示、Copilot 申請頁、文件審核畫面、LINE 訊息的**真實截圖**。截圖下只留一行動詞：提醒、填寫、比對、通知。
- **台詞（25 秒）：**「作品有兩個相互補充的入口：瀏覽器提示帶到 Copilot，LINE 帶到文件申請與審核服務。兩邊都讓使用者理解申請、讓承辦人看見依據；我們等一下會分別展示。」
- **證據：**避免用一張只有箭頭的架構圖代替真實產品畫面。

### 第 3 頁｜兩條實作路徑與共用的 OCR 模組

- **畫面：**使用上面的「路徑 A／路徑 B」精簡圖。標出 :3000、:8000、:3001；註明「兩套案件資料，OCR 程式可共用」。不要畫成 LINE 直接讀取 Copilot 案件。
- **台詞（35 秒）：**「擴充功能可把產品名稱帶到 Copilot。LINE 的申請與進度查詢則對應 OCR 服務自己的案件；Copilot 的完整文件審核會使用 OCR 專案的模組。今天 Demo 會分別給大家看兩條路徑。」

### 第 4 頁｜AI 聊天：讓政策回答找得到依據

- **畫面：**左側一張聊天回答與引用來源的截圖；右側只有五步：`政策 Markdown → 分段與索引 → 問題檢索 → 根據段落回答 → 顯示引用`。
- **台詞（35 秒）：**「政策內容先切成段落並建立檢索索引。民眾提問時，系統挑出相關段落，再生成有來源的說明；找不到足夠依據時應說無法確認。聊天只協助解釋，不直接修改資格或撥款。」
- **技術備註：**Copilot 使用 PostgreSQL／pgvector 與文字訊號做檢索；若未配置外部 embedding，可使用可重現的本機向量表示。生成端可依設定走 Gemini 或 OpenAI，也有離線示範回覆。投影片不要寫成「所有回答都由 Gemini」或「所有情況都即時連官方資料」。
- **建議現場提問：**`Is ChatGPT Plus eligible?`，停在政策引用卡片，指出具體條文。

### 第 5 頁｜AI 資安教育嵌入送件流程

- **畫面：**申請步驟中「AI Safety Training required」與完成 `4/4` 的前後截圖。旁邊列四主題：隱私、幻覺、Prompt Injection、人的責任。
- **台詞（30 秒）：**「使用者不是看完一篇文章就算完成：每個單元要作答，後端保存分數與完成狀態；四題未完成，送件會被擋下。答錯可以重試。」
- **必講清楚：**這四題是預先設計並由後端判分的題目，**不是 RAG 即時生成題目**。RAG 主要用於政策問答，知識庫也包含資安主題文字；課程與送件門檻則是確定性的流程控制。

### 第 6 頁｜AI 的權限邊界

- **畫面：**三列即可：`AI：回答與擷取`、`規則：資格與風險`、`承辦人／受控服務：例外核定與示範付款`。每列配一張小證據：引用、逐項規則、審核紀錄。
- **台詞（25 秒）：**「我們把自然語言的方便性放在說明和資料擷取；會影響資格與款項的步驟仍經後端規則、狀態檢查和人工複核。」
- **避免說過頭：**Copilot 的低風險完整案件可能由**規則工作流程**自動核准；OCR／LINE 後台的 PASS 等燈號只是承辦人建議。兩套流程不可混稱「全部一定人工核准」。付款畫面是 mock payment，不是銀行匯款。

### 第 7 頁｜上傳文件：OCR 擷取後逐項交叉比對

- **畫面：**用一個例子呈現 `申請人填的金額`、`收據擷取金額`、`比對結果` 三欄；旁邊放後台的規則清單或交叉比對矩陣截圖。可選擇 `DEMO-FRAUD-01`，放大存摺戶名不一致或重複憑證的規則。
- **台詞（40 秒）：**「文件上傳後，Gemini 擷取欄位與信心分數；程式把表單、身分證、收據、存摺及工具清單對照，再執行適用的 RULE-001 到 RULE-020。辨識失敗會標成無法確認或需補件，不假裝通過；疑點由承辦人看原始文件與理由。」
- **展示時指順序：**若有現場上傳案件，先指原始文件，再指 OCR 欄位 → 比對矩陣 → 某一條規則的理由 → 案件建議。不要直接從紅燈跳到「AI 發現詐欺」；應說「疑似異常，需人工查核」。
- **資料準確性：**`seed-demo.js` 的三筆示範案直接餵入**模擬的 OCR 欄位**再跑真正的規則引擎，且沒有附原始文件檔，因此可以證明規則與後台顯示，不能宣稱這三筆是現場 Gemini 辨識出的結果。若要證明即時 OCR，另用已準備好的虛構圖片上傳，並預留延遲。

### 第 8 頁｜LINE Bot：申請入口、進度查詢、狀態通知

- **畫面：**手機截圖三格：傳「申請」得到連結、傳「查詢進度」得到案件狀態、承辦人更新後收到通知。若當天尚未接上 LINE 平台，改用已拍好的錄影或截圖，並清楚標示來源。
- **台詞（35 秒）：**「LINE webhook 收到訊息後，固定指令直接走程式邏輯：申請會帶使用者 ID 進 OCR 申請頁，查詢則讀取該 LINE 身分對應的案件；送件與狀態變更可以推播。其他政策問題才交給 Gemini，並附上專案維護的知識內容。」
- **技術重點：**使用 `@line/bot-sdk` 的 webhook middleware 驗證 LINE 請求。LINE 的自然語言政策答覆是把 `knowledge-base.js` 放進提示詞的方式，**不是 Copilot 那套 pgvector RAG**。

### 第 9 頁｜Chrome 擴充功能：在使用 AI 的當下提醒

- **畫面：**左邊 ChatGPT 網站出現「可能符合補助、立即申請」；右邊示範風險網站的警示畫面。再用一句話標出輸入內容提醒：「疑似個資或 Prompt Injection → 送出前警告」。
- **台詞（35 秒）：**「擴充功能在網頁端依網域清單分類。符合內建對照的工具會顯示申請入口；高風險或未核准網域會提醒。它也以格式和關鍵字檢查 AI 輸入框，發現敏感資料時先讓使用者確認。」
- **用語：**名稱請寫 **Chrome 擴充功能**或**瀏覽器擴充功能**；這不是 Google 官方發行的擴充功能，也不是由模型即時認證網站安全。黑名單與白名單是專案內的靜態資料，應說「依示範清單提醒」，不要保證所有網站的真實資格。

### 第 10 頁｜擴充功能如何帶到申請頁

- **畫面：**僅放一個可讀的 URL 例子：`/apply?product=ChatGPT%20Plus`，以及選擇產品後網站畫面的截圖。下面用小字寫「content script → 網域對照 → URL 參數 → 申請頁預選產品」。
- **台詞（25 秒）：**「它沒有替民眾送件，而是先判斷現在網站對應哪個產品，再把產品名放進申請網址；使用者仍須登入、上傳證明、完成安全課程與資格檢查。」
- **注意：**目前擴充功能的產品表比 Copilot 示範政策列的三項產品更多；現場固定示範 **ChatGPT Plus**，其他產品不宣稱均已在 Copilot 中可申請。

### 第 11 頁｜Demo 路線圖

- **畫面：**四個大編號，不放長文：`1 提示 → 2 政策與申請 → 3 審核依據 → 4 LINE 查詢`。每完成一步，回指這頁的相應位置。
- **台詞（10 秒）：**「接下來只看四個證據畫面；每一步都回答：誰做了什麼、根據什麼、結果在哪裡。」

### 第 12 頁｜結尾：讓決定有依據，也能追蹤

- **畫面：**一句結論加三個已展示的證據：「政策引用」「規則理由」「案件時間軸／LINE 通知」。右下角保留「示範系統」標記。
- **台詞（20 秒）：**「我們展示的是一個能引導民眾、檢查資料並留下審核依據的流程。AI 幫忙閱讀與說明；資格由規則檢查，例外由承辦人處理。下一步是整合兩套案件資料與正式身分驗證。」

## 三、現場 Demo：照這個順序操作

### 上台前準備（最好前一天完整彩排一次）

1. 在專案根目錄執行 `./demo-start.sh`；確認 `http://localhost:3000`、`http://localhost:3001/admin.html`、`http://localhost:8000/health` 可開。執行前不要先在觀眾面前等待 Docker 啟動。
2. 到 `chrome://extensions` 確認已載入 `黑克嵩擴充功能_合併終版`；準備 `chatgpt.com` 分頁及網站分頁。瀏覽器登入與網路情況可能影響第三方頁面，預先錄一段 20 秒備援影片。
3. Copilot 的收據示範檔放在手邊：`ai-subsidy-copilot/demo/receipts/chatgpt_plus_valid.pdf`。用 `Alex Chen` 跑一遍完整流程，拍好引用來源、四題完成、資格規則三張備援截圖。正式上台前按 [DEMO_GUIDE.md](DEMO_GUIDE.md) 重置 Copilot 示範資料，並確認 Alex 回到新申請狀態。
4. 到 `OCR` 目錄執行 `node seed-demo.js`，預先登入 `http://localhost:3001/admin.html`，確認 `DEMO-FRAUD-01`、`DEMO-SUPP-01` 出現。後台 Token 從 `OCR/.env` 的 `ADMIN_TOKEN` 讀取，請勿放上投影片或錄影。
5. 若要**即時操作手機 LINE**，事先確認 LINE channel、`PUBLIC_BASE_URL`、可公開存取的 HTTPS tunnel 與 `/webhook` 已設定。從同一個測試帳號傳「申請」，點連結在 :3001 申請頁**送出一筆測試案件**，再傳「查詢進度」確認能找到它。`seed-demo.js` 的案件沒有綁定 LINE user ID，查詢時不會出現在測試帳號下。LINE 回的 `apply.html` 是 **:3001 服務**；無外部 tunnel 時，手機無法打開台上電腦的 `localhost`。不要現場才設定。
6. 桌面只留必要分頁並隱藏通知；以 125% 左右的瀏覽器縮放檢查後排是否看得見。把 demo 檔案放在容易選到的目錄，並準備滑鼠游標可見的備援錄影。

### 5 分鐘主流程（建議兩人配合：一人講、一人操作）

| 時間 | 操作 | 一句講法 | 讓觀眾看到的證據 |
| --- | --- | --- | --- |
| 0:00–0:40 | 在 `chatgpt.com` 看擴充功能提示，點「立即申請」 | 「在使用工具時提醒，點一下就把產品帶入申請。」 | 提示與 `/apply?product=...` |
| 0:40–1:35 | 進 Copilot，以 Alex 登入；看 ChatGPT Plus 與政策問答引用 | 「政策回答有來源；產品仍須進入正式表單檢查。」 | 預選產品、引用卡片 |
| 1:35–2:30 | 上傳示範 PDF，確認擷取欄位；看資格規則與 AI Safety | 「收據是證據；四個資安單元要完成才能送件。」 | 擷取欄位、逐項規則、4/4 狀態（可用已彩排截圖快轉） |
| 2:30–3:35 | 切到 OCR 後台的 `DEMO-FRAUD-01` | 「這是另一個案件流程的承辦人視角：看預置的擷取欄位、比對結果與每條規則的理由。」 | 規則、交叉矩陣、時間軸；指出 seeded OCR 欄位 |
| 3:35–4:35 | 手機 LINE 傳「申請」「查詢進度」，或播預錄 | 「LINE 提供入口與這套 OCR 案件的進度。」 | 回覆的 `apply.html` 連結與狀態；若未連線就展示截圖 |
| 4:35–5:00 | 回第 12 頁 | 「提醒、申請、審核、通知都有實際可見的依據。」 | 三個已展示的證據 |

**Demo 路線切換的必說句：**「剛才是 Copilot 的 Alex 示範申請；現在切到 LINE／OCR 服務的另一筆 `DEMO-FRAUD-01` 案件，展示承辦人能查到的規則與資料來源。」這句可以避免觀眾以為兩邊顯示的是同一筆案件。

若評審想看**完整送件**，把上表第 2～3 段延長：完成四個單元，回申請頁按 `Submit application`，再開 Copilot 的 `/admin` 與追蹤頁。這是 Copilot 自己的管理後台，和 :3001 的 OCR／LINE 後台要分開介紹。只在有時間時展示 mock payment，並明說那是虛構交易。

### 失敗時的接法

| 情況 | 當下處理與講法 |
| --- | --- |
| 第三方 AI 網站或擴充功能提示沒出來 | 播預錄 20 秒，接著手動開 `http://localhost:3000/apply?product=ChatGPT%20Plus`；說「這段錄影顯示擴充功能的入口」。 |
| AI 回答較慢或 API 暫時不可用 | 直接展示事先截好的政策引用；繼續表單。說「模型協助說明，資格檢查仍能由後端流程執行」。 |
| 即時 Gemini OCR 無回應 | 使用 seeded OCR 後台案件展示**規則比對**，明說其中 OCR 欄位是預置示範資料；不要說是剛剛辨識出的文件。 |
| LINE webhook／手機連結失敗 | 播同一測試帳號的預錄對話，展示 :3001 的申請頁與後台；說明 live LINE 依賴外部 webhook 連線。 |
| 超時 | 跳過完整四題作答與 mock payment，直接展示 `4/4` 截圖、規則理由與結尾。 |

## 四、怎麼讓成果看得清楚

- **每個技術點配一個可看見的結果。**RAG 對應「回答旁的引用」；資安門檻對應「未完成不能送件／完成 4/4」；OCR 對應「原始文件與擷取欄位」；規則對應「可展開的理由」；LINE 對應「同一帳號的案件狀態」。
- **先講結果，再講機制。**例如先指著「存摺戶名與申請人不一致」，再說 OCR 擷取及交叉比對；觀眾比較容易跟上。
- **一張投影片只放觀眾要看的關鍵畫面。**程式碼最多一行（例如 URL 參數或 RAG 流程）；完整 `rules.js` 與 API 列表留給 Q&A。
- **數字只用有來源的數字。**可說「20 條規則中執行適用項目」「4 個必修單元」「Copilot 示範政策中的 7 項資格檢查」。OCR 後台的「節省審核時間」是固定假設試算，不是實測成果。
- **不要混用兩份示範政策。**Copilot `knowledge/` 是虛構 demo 政策，LINE `knowledge-base.js` 使用另一套維護內容；它們的年齡、金額與合格產品範圍不完全相同。簡報聚焦流程與技術，政策數值只在對應畫面中解釋，別宣稱全系統已統一政策。
- **避開敏感資料。**只用專案虛構人員、示範收據與測試 LINE 帳號；截圖裁掉 webhook 金鑰、管理 Token、真實 LINE user ID 與通知預覽。

## 五、評審可能問的問題：簡短回答

**Q：你們的資安題目是 RAG 做出來的嗎？** 不是。RAG 用來從政策及安全知識段落找出有來源的回答。四個必修資安單元與答案是預先建立的，後端記錄進度並阻擋未完成者送件。

**Q：AI 會自己核准或退件嗎？** OCR／LINE 後台的五種結果是建議，最終由承辦人操作；需補件可自動通知。Copilot 的低風險完整案件可由確定性規則工作流程核准，這不是讓語言模型決定。付款是受狀態限制的示範服務。

**Q：辨識錯誤怎麼辦？** 保留原始文件、擷取欄位與信心分數供核對；無法辨識時走補件或人工確認。OCR 的結果不直接等於最終資格。

**Q：LINE 和網站是不是同一份資料？** LINE 對應 :3001 OCR 申請服務的案件；Chrome 擴充功能帶到 :3000 Copilot。Copilot 可共用 OCR 模組，但目前兩套案件資料沒有自動同步。

**Q：擴充功能如何判斷工具？** 使用靜態網域清單、產品對照及輸入文字的格式／關鍵字檢查；它是示範性的前端提醒，不是官方核准狀態的即時查詢。

**Q：你們真的已經發補助款嗎？** 沒有。Copilot 的付款編號與金額流轉是 mock payment，用於展示核准後的狀態檢查與可追溯紀錄。

## 製作投影片時可直接用的專案素材

- [總 Demo 操作指南](DEMO_GUIDE.md)：一鍵啟動、網址與重置。
- [Copilot README](ai-subsidy-copilot/README.md)、[五分鐘 Demo 腳本](ai-subsidy-copilot/demo/DEMO_SCRIPT.md)：RAG、收據、課程、追蹤。
- [OCR／LINE README](OCR/README.md)、[示範案件產生器](OCR/seed-demo.js)：文件審核與三筆可展示案件。
- [LINE 實作](OCR/line.js)、[Chrome 擴充功能實作](黑克嵩擴充功能_合併終版/content.js)：說明路徑時核對用。
- [Copilot 示範政策](ai-subsidy-copilot/knowledge/ai_subsidy_policy_2026.md)：投影片若需要條文引用，使用此檔並標「虛構示範政策」。

**最後彩排標準：**不看稿能在 20 秒內說清兩條路徑；每張技術頁有一張對應證據；5 分鐘 Demo 不需要臨時找檔案；任何一項外部連線失敗都能在 10 秒內切到備援畫面。
